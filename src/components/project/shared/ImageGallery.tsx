import LazyImage from "@/components/ui/LazyImage";

// Replace any img tags with LazyImage
// For example:
// <img src={image.url} alt={image.caption || `Image ${index + 1}`} loading="lazy" className="..." />
// becomes:
// <LazyImage src={image.url} alt={image.caption || `Image ${index + 1}`} className="..." /> 